# БибиЗадачи v2.4.0 — анкета, пользователи, шаблоны и фото

Заменить нужно два файла: `main.py` и `index.html`. База не ломается —
новые колонки и таблицы создаются сами, старые данные остаются.

## Что добавлено в v2.4.0

- В регистрации теперь только имя, город и свободный комментарий о том,
  что человек сможет выполнять и чем будет полезен компании. Телефон больше
  не запрашивается; старые номера из базы автоматически не удаляются.
- В «Модерация → Пользователи» появились поиск по имени, городу, нику,
  комментарию и тегам, фильтр по тегу и сортировка по делам, имени, городу
  или балансу.
- Ответственный может открыть карточку человека и назначить ему до 12 тегов,
  например `парковки`, `фото`, `автомобиль`. После этого человека легко найти
  и сразу начислить ему бибибонусы; в причинах начисления есть готовые варианты.
- В редакторе задания появились четыре шаблона: парковка, фото-проверка,
  перестановка байков и замена батарей.
- К заданию можно прикрепить JPG, PNG или WebP. Браузер уменьшает фотографию,
  сервер повторно проверяет формат и размер, а фото показывается во всех
  карточках задания. Файлы хранятся в `DATA_DIR/task_photos`, поэтому на
  хостинге для `DATA_DIR` нужно сохранить постоянный диск так же, как для базы.

---

## Изменения предыдущих версий

---

## ⚠️ Сначала важное: bbbikefan — это группа, а не бот

Ссылка `https://t.me/bbbikefan?start=ref_<id>` **не работала бы**.
Диплинк `?start=` понимают только боты — у группы он просто откроет группу
и приглашение не засчитается. В прошлый раз я поставил `bbbikefan` как
`BOT_USERNAME` с твоих слов, и это было ошибкой. Теперь развёл:

| Что | Адрес | Где используется |
|---|---|---|
| Бот | `BbGalterbot` | реферальные ссылки `?start=ref_<id>` |
| Mini App | `BbGalterbot/bibibike` | кнопка «Открыть задания» под сообщениями |
| Группа | `bbbikefan` | проверка подписки, подтемы |

Проверь свою реферальную ссылку: она теперь
`https://t.me/BbGalterbot?start=ref_7785586524`.

---

## 1. Кнопка под сообщениями бота

Теперь ведёт на `https://t.me/BbGalterbot/bibibike`.
Меняется переменными `BOT_USERNAME` и `WEBAPP_SHORTNAME`, но значения по
умолчанию уже правильные — трогать не нужно.

---

## 2. Опыт за общение → уровень доверия

Работает в подтеме беседы (`/3`).

| Действие | Опыт | Почему так |
|---|---|---|
| Обычное сообщение | **+1** | мелкий вес, как ты и просил |
| Тебе сказали спасибо | **+15** | крупный вес, начисляется получателю |

**50 опыта = одно выполненное задание** при расчёте уровня. То есть уровень
теперь `задания + опыт÷50`. При нулевом опыте формула даёт ровно старый
результат — ни у кого уровень не съедет после обновления.

Пороги прежние: 🌱 Новичок → ⭐ Проверенный (10) → 👑 Амбассадор (40).
Значит «Проверенный» — это 10 заданий, или 500 опыта, или любая смесь.

«Спасибо» ловится по корням: спасиб, спс, спсб, благодар, признателен,
выручил, респект, thanks, thx, 🙏. Обязательно **ответом** на сообщение
человека — иначе непонятно, кого благодарят.

Защита от накрутки, всё настраивается переменными:

- сообщение короче 4 символов не считается (`MSG_MIN_CHARS`);
- не чаще одного зачёта в 60 секунд (`MSG_COOLDOWN_SEC`);
- потолок 20 опыта в день за сообщения (`MSG_XP_DAILY_CAP`);
- потолок 45 опыта в день за спасибо (`THANKS_XP_DAILY_CAP`);
- одна пара «кто кого благодарит» — раз в 12 часов
  (`THANKS_PAIR_COOLDOWN_H`);
- себе, ботам и командам опыт не идёт.

Пропорция задаётся `CHAT_XP_PER_TASK` (по умолчанию 50). Хочешь, чтобы
болтовня весила меньше — поставь 100. Больше — поставь 25.

В профиле приложения под шкалой теперь видно, из чего собран уровень:
«4 задания + 130 опыта в беседе (это ещё +2)».

---

## 3. Значок уровня рядом с человеком в чате

Тут придётся сказать прямо: **так, как в профиле приложения, сделать
нельзя.** У Telegram нет API, чтобы бот дописал значок к имени участника в
чате. Единственное, что меняет подпись — custom title, и он доступен только
администраторам группы и ограничен 16 символами. Ставить всех подряд
админами ради значка — плохая идея.

Что сделал вместо этого, три вещи:

1. **Объявление о росте уровня** — когда человек переходит на новый
   уровень, бот пишет в ту же подтему: «⭐ Эльдар — новый уровень доверия:
   Проверенный». Редко, заметно, не спамит.
2. **Команда `/ранг`** (также `/уровень`) — работает и в группе, и в личке.
   Показывает значок, уровень, задания, опыт и сколько до следующего.
3. **Реакция 🙏** на сообщение со спасибо — видно, что бот его засчитал.

Если хочешь именно постоянный значок, есть обходной путь: попросить людей
самим добавить эмодзи в имя. Автоматизировать это бот не может.

---

## 4. Посты в подтемы — командами

Четыре команды для ответственных. Работают двумя способами: написать
в самой подтеме — бот опубликует туда же; написать боту в личку — бот сам
отправит в нужную подтему и подтвердит.

| Команда | Куда | Что публикует |
|---|---|---|
| `/франшиза` | подтема 43 | условия, эксклюзив, платформа, техника, рынок, FAQ |
| `/болталка` | подтема 3 | правила, как работает опыт, список команд |
| `/новости` | подтема 1 | навигация по группе, про подписку, про приложение |
| `/инструкция` | подтема 4 | как регистрироваться и работать |

Длинные посты бот сам режет по абзацам под лимит Telegram: франшиза уходит
тремя сообщениями, болталка двумя, новости одним. Кнопки вешаются
на последнее сообщение.

**Каждый пост публикуется один раз.** Повторный вызов команды ничего
не отправит — бот ответит, что пост уже опубликован, когда именно
и даст на него ссылку. Дублей в подтеме не будет, даже если случайно
нажать команду дважды.

Чтобы заменить пост новой версией, добавь к команде слово **заново**:

```
/франшиза заново
```

Тогда бот удалит старые сообщения этого поста и опубликует свежие.
Работают и `переопубликовать`, `force`, `-f`. Каждый из четырёх постов
считается отдельно, регистр значения не имеет.

**Ссылки и кнопки:**

- под франшизой — «Условия и калькулятор» на start.bb.bike и «Открыть
  приложение»; в тексте кликабельны сайт и почта `partner@bb.bike`;
- под новостями — App Store, Google Play и «Задания и бонусы»;
- в новостях подтемы даны ссылками на `t.me/bbbikefan/1`, `/3`, `/4`, `/43`;
- везде, где есть приложение, ведёт на `t.me/BbGalterbot/bibibike`.

Адреса магазинов, сайта и почты выносятся переменными `APP_STORE_URL`,
`GOOGLE_PLAY_URL`, `FRANCHISE_URL`, `PARTNER_EMAIL` — менять можно без
правки кода.

---

## 4.1. Приветствие новичков

Служебное «X вступил в группу» удаляется, вместо него бот пишет в болталку:

> Эльдар Гематденов, добро пожаловать в фан-клуб Бибибайка 🚲

Дальше короткая подсказка про уровень доверия и `/команды`, плюс кнопка
на приложение. Если зашли несколько человек разом — одно сообщение на всех.
Ботов не приветствует. Имена экранируются, так что ник со спецсимволами
ничего не сломает.

Для удаления служебного сообщения боту нужно право «Удаление сообщений».
Если права нет — приветствие всё равно отправится, просто служебная строка
останется.

---

## 4.2. Исправления по твоему скриншоту

**«Bad Request: message thread not found».** Подтема «Новости» — это
General-ветка форума. У неё thread_id = 1, но Telegram не принимает эту
единицу в `message_thread_id`: в General нужно отправлять вообще без
указания подтемы. Теперь бот так и делает. Плюс подстраховка: если любая
подтема вдруг не найдётся, он повторит отправку без неё, а не свалится
с ошибкой.

**`/Новости` с заглавной буквы молчали.** Мои регулярки были чувствительны
к регистру, а мобильная клавиатура автоматически поднимает первую букву.
Теперь все команды регистронезависимы: `/новости`, `/Новости`, `/НОВОСТИ`
и `/News` работают одинаково. Вариант с упоминанием бота
(`/новости@BbGalterbot`) тоже.

**Новая команда `/подтема`** (синонимы `/topic`, `/id`), только для
ответственных. Зайди в любую подтему, напиши её — бот покажет chat_id,
настоящий id подтемы, как она сейчас настроена и, главное, распознаёт ли
он эту группу вообще.

Последнее важно: на скриншоте видно, что служебные «теперь в группе»
не удалились. Команды бот получает всегда, а вот приветствие, опыт
за общение и защита франшизы работают только если группа распознана
по `GROUP_USERNAME`. Если username не совпадает буква в букву — всё это
молча не работает. `/подтема` покажет прямо, так это или нет, и подскажет
готовую строку `GROUP_ID=...`.

Если группа распознаётся, а служебные сообщения всё равно висят — значит
боту не хватает права «Удаление сообщений».

---

## 4.3. Команды участников

| Команда | Что показывает |
|---|---|
| `/профиль` | уровень и бибибонусы одним сообщением |
| `/ранг` | уровень доверия, опыт, сколько до следующей ступени |
| `/бонусы` | баланс и сколько не хватает до обмена |
| `/задания` | кнопка в приложение |
| `/подтема` | только для ответственных: диагностика группы и подтем |
| `/команды` | список; ответственным дополнительно показывает админские |

Синонимы: `/уровень`, `/баланс`, `/profile`, `/help`. Работают и в группе,
и в личке.

---

## 5. Подтема «Работа»: инструкция

Команда **`/инструкция`** — для ответственных. Работает двумя способами:

- написать её прямо в подтеме «Работа» → бот опубликует туда;
- написать боту в личку → бот сам отправит в подтему `TOPIC_WORK`.

Текст лежит в `WORK_INSTRUCTION` в `main.py` — правь как хочешь. Он
покрывает регистрацию, как брать и сдавать задания, бибибонусы, уровень
доверия и приглашение друзей. Под сообщением — кнопка прямого перехода в
бота.

---

## 6. Подтема «Франшиза»: только одобренные

В подтеме `/43` бот удаляет сообщения тех, кто не прошёл одобрение, и
пишет человеку в личку, почему так и что делать. Администраторов группы
не трогает.

Чтобы это работало, **боту нужно право «Удаление сообщений»** в группе.
Если права нет — бот молча пропустит сообщение и напишет об этом в лог,
ничего не сломается.

---

## 7. Что нужно настроить на хостинге

```
REQUIRED_CHAT=@bbbikefan       # уже стоит по умолчанию
GROUP_USERNAME=bbbikefan       # уже стоит по умолчанию
TOPIC_NEWS=1
TOPIC_CHAT=3
TOPIC_WORK=4
TOPIC_FRANCHISE=43
```

Всё это уже прописано значениями по умолчанию — если id подтем не менялись,
добавлять ничего не нужно.

**Права бота в группе.** Проверь три вещи, иначе часть механик молча не
заработает:

1. **Бот — администратор группы.** Нужно и для проверки подписки, и чтобы
   он видел все сообщения.
2. **Право «Удаление сообщений»** — для подтемы франшизы.
3. **Privacy mode выключен** у @BotFather (`/setprivacy` → Disable), если
   бот не админ. Если админ — не нужно, админы видят всё.

---

## Проверено

- **84 проверки API** на живом сервере с настоящей подписью Telegram.
- **121 проверка групповой логики** на поддельных сообщениях: опыт за
  сообщения и потолки, кулдауны, все формы «спасибо» и все способы их
  накрутить, рост уровня, удаление во франшизе, обход для админов группы,
  границы подтем и чужих групп, приветствие новичков с экранированием имён,
  публикация всех четырёх постов в правильные подтемы и запрет публикации
  для не-ответственных, все команды участников, отправка в General без
  подтемы и повтор при ненайденной подтеме, распознавание команд в любом
  регистре и с упоминанием бота, диагностика группы, однократная
  публикация с удалением прошлой версии при переопубликации.
- **136 проверок интерфейса** в браузерном окружении.

Итого 341, все зелёные.

---

## Что осталось нерешённым осознанно

- Подтверждение задания по-прежнему держится на «ОК» ответственного —
  гео и фото это следующий шаг.
- Значок уровня в чате — ограничение Telegram, см. раздел 3.
- Опыт капает только в подтеме беседы. Если захочешь считать и новости —
  скажи, это одна строка.
