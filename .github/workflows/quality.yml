name: quick-check

on:
  push:
    branches: ["main"]
  pull_request:
  workflow_call:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  syntax:
    runs-on: ubuntu-latest
    timeout-minutes: 3
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.12"
      - name: Быстрая проверка синтаксиса
        run: python -m compileall -q main.py scripts migrations db_migration
      - name: Контракт входа и прав доступа
        run: python -m unittest discover -s tests -v
